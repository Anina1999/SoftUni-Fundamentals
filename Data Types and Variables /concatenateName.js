function concatenateName(firstName, secondName, delimiter) {

    //let result = firstName + delimiter + secondName;
    
    console.log(`${firstName}${delimiter}${secondName}`);

}

//concatenateName('John', 'Smith', '->');
